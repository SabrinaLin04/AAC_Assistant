<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/aac_surface_container"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/aac_surface_container"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/card_padding">

        <HorizontalScrollView
            android:id="@+id/pictogramScroll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/bg_pictogram_strip"
            android:paddingHorizontal="10dp"
            android:paddingVertical="10dp"
            android:scrollbars="horizontal"
            android:visibility="visible">

            <LinearLayout
                android:id="@+id/pictogramRow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="horizontal" />

        </HorizontalScrollView>

        <TextView
            android:id="@+id/speakText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/card_padding"
            android:gravity="center"
            android:textAppearance="@style/TextAppearance.App.Display" />

        <!-- sta qui e non tra i pulsanti del dialog: in fondo ce ne stanno solo due -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/shareButton"
            style="@style/Widget.AAC.Button.Outlined"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/card_padding"
            android:text="@string/action_share"
            android:visibility="gone"
            app:icon="@drawable/ic_share"
            app:iconGravity="textStart" />

    </LinearLayout>
</ScrollView>
